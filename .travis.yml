dist: trusty
sudo: false

language: node_js
node_js:
  - '8'

cache:
  directories:
    - ./node_modules

install:
  - npm install

script:
  - npm run lint
  - npm install . -g
  - pbiny -h
  - pbiny up -h
  - pbiny usr